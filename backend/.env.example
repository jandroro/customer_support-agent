# Copy to .env to override config.yaml values without editing it.
# Any setting can be overridden using its nested path joined with "__".

# CONFIG_FILE=config.yaml
# AWS__PROFILE=your-aws-profile
# AWS__REGION=us-east-1
# COGNITO__TEST_PASSWORD=choose-your-own-password
