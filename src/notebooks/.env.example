# Copy to .env (same directory as this file).
# Overrides the MCPServerPool test-user credentials used by
# lab_helpers.utils.get_or_create_cognito_pool() — falls back to the
# previous hardcoded defaults (testuser / MyPassword123!) if unset.

COGNITO_USERNAME=testuser
COGNITO_PASSWORD=MyPassword123!
